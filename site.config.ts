type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "for spm",
  site_description: "Starter template for Headless WordPress with Next.js for SPM",
  site_domain: "https://spmiasacademy.com",
};
